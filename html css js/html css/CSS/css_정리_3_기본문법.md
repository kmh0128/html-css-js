css 기본문법
===

참고자료
---

https://inpa.tistory.com/entry/CSS-📚-css-기본-문법-정리

https://wikidocs.net/288013

css 기본문법 구조
---

            선택자 {
             속성: 값;
             속성: 값;
            }

선택자 - CSS를 적용하고자 하는 HTML 요소(element)

선언부 - 하나 이상의 선언들을 세미콜론(;)으로 구분하여 포함할 수 있으며, 중괄호({ })를 사용하여 전체를 둘러쌉니다.

선언은 CSS 속성명(property)과 속성값(value)을 가지며, 그 둘은 콜론(:)으로 연결됩니다. 

CSS 선언(declaration)은 언제나 마지막에 세미콜론(;)으로 끝마친다.

위와 같은 구조를 CSS 규칙(Rule) 또는 룰셋(Ruleset)이라고 부른다.

선택자(Selector)
---

위에 써놓은 것처럼 HTML 요소(element)선택하는 방법을 정의합니다.

선언(declaration)
---

속성: 값; 형태로 구성된 하나의 스타일 지시사항


속성(Property)
---

"무엇을" 바꿀 것인지 지정합니다. (예: color, font-size, background)



값(Value)
---
속성을 "어떻게" 바꿀 것인지 구체적으로 명시합니다. (예: red, 16px, center)
